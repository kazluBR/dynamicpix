# This is a basic workflow to help you get started with Actions

name: CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Build running!
        
  deploy-dev:
    runs-on: ubuntu-latest
    environment:
      name: dev
      url: www.dev.com.br
    needs: build
    steps:
      - name: Run a one-line script
        run: echo Deploy Dev running!
  
  deploy-staging:
    runs-on: ubuntu-latest
    environment:
      name: staging
      url: www.staging.com.br
    needs: [build, deploy-dev]
    steps:
      - name: Run a one-line script
        run: echo Deploy Staging running!
      
  deploy-prod:
    runs-on: ubuntu-latest
    environment:
      name: prod
      url: www.prod.com.br
    needs: [build, deploy-staging]
    steps:
      - name: Run a one-line script
        run: echo Deploy Prod running!
